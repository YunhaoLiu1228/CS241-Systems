/**
 * vector
 * CS 241 - Fall 2020
 */
#include "sstring.h"
#include <string.h>

int main(int argc, char *argv[]) {
    // char* input = "this is a char pointer";
    // sstring* str = cstr_to_sstring(input);
    
    // // printf("sstring: ");
    // // for (size_t i = 0; i < str->length; i++) {
    // //     printf("%c", **(char**)vector_at(str->string, i));
    // // }
    // // printf("\n");

    // char* output = sstring_to_cstr(str);
    // printf("c_string: '%s' \n", output);
    // printf(" // ---------------------------------------- // \n");

    // // -------------- test append ---------------- //
    // char* input2 = " hiyaaa";
    // sstring* str_to_append = cstr_to_sstring(input2);
    // sstring_append(str, str_to_append);

    // char *cstr = sstring_to_cstr(str);
    //  for (size_t i = 0; i < strlen(cstr); i++) {
    //     printf("%c", cstr[i]);
    // }
    // printf("\n");
    // printf(" // ---------------------------------------- // \n");

    // // --------------- test split ---------------- //

    // char* strljs = "here is another";
    // printf("Splitting string: %s\n", strljs);
    // sstring* str_ = cstr_to_sstring(strljs);
    // vector* subs = sstring_split(str_, ' ');

    // for(size_t i = 0; i < vector_size(subs); i++) {
    //     printf("substr: %s\n", *(char**)vector_at(subs, i));
    // }

    // char* strlja = "some/strings/to/split";
    // printf("Splitting string: %s\n", strlja);
    // sstring* strs_ = cstr_to_sstring(strlja);
    // vector* subss = sstring_split(strs_, '/');

    // for(size_t i = 0; i < vector_size(subss); i++) {
    //     printf("substr: %s\n", *(char**)vector_at(subss, i));
    // }
    // printf(" // ---------------------------------------- // \n");

    // // ---------------- test slice ---------------- //

    return 0;
}
